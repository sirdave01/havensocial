<%- include('partials/header') %>

    <main class="feed-page">
        <div class="compose-tweet">
            <form action="/tweets" method="POST" id="tweetForm" enctype="multipart/form-data">
                <textarea name="content" id="tweetContent" placeholder="What's happening?" maxlength="280"
                    required></textarea>

                <!-- Media Upload -->
                <div class="media-upload-area">
                    <label for="tweetMedia" class="upload-label">
                        📎 Attach media
                    </label>
                    <input type="file" id="tweetMedia" name="media" accept="image/*,video/*" style="display: none;">
                    <div id="mediaPreview" class="media-preview"></div>
                </div>

                <div class="compose-actions">
                    <button type="button" class="attach-btn" onclick="document.getElementById('tweetMedia').click()">
                        📎
                    </button>
                    <button type="submit" class="btn-primary post-btn">Post</button>
                </div>
            </form>
        </div>

        <div class="feed" id="feedContainer">
            <% if (feed && feed.length> 0) { %>
                <% feed.forEach(tweet=> { %>
                <%- include('partials/tweet-card', { tweet }) %>
                <% }); %>
            <% } else { %>
                <p class="empty-feed">No posts yet. Follow more people to see updates!</p>
            <% } %>
        </div>
    </main>

    <%- include('partials/footer') %>